export default function Footer() {
  return (
    <footer className="mt-auto border-t border-hairline">
      <div className="mx-auto flex max-w-6xl flex-col gap-1 px-6 py-8 text-xs text-muted sm:flex-row sm:items-center sm:justify-between">
        <p>Resume Matcher — skills-based hiring.</p>
        <p>Matches are AI-assisted. Review them before acting.</p>
      </div>
    </footer>
  );
}
